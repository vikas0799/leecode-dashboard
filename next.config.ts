const nextConfig = {
  // Disable the red Next.js dev indicator button
  devIndicators: {
    // @ts-ignore - property is supported by Next.js but not in this TS type
    buildActivity: false,
  },
};

export default nextConfig;
